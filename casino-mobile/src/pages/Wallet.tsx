import { defineComponent } from 'vue'

export default defineComponent(() => {
  return () => (
    <section class="page">
      <h1 class="page-title">Wallet</h1>
      <div class="card">
        <div class="card-title">Balance</div>
        <div class="card-body">Connect your API later (services + stores).</div>
      </div>
    </section>
  )
})
